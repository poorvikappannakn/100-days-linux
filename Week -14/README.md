# Week 14 – Progress

## 📅 Status

✅ Week 14 Completed

## 📖 Overview

This week focused on shell environment management and configuration, including environment variables, child shells, process identification, shell aliases, shell options, `allexport`, `SHELLOPTS`, and persistent shell configuration using `.bashrc` and `.zshrc`.

## 📚 Topics Covered

### ✅ Environment Variables and Processes

- Local variables
- Environment variables
- Variable inheritance
- Child shells
- Process ID (PID)
- Parent Process ID (PPID)
- Returning to the parent shell

### ✅ Shell Aliases

- Creating aliases
- Using aliases as command shortcuts
- Verifying aliases
- Removing aliases using `unalias`

### ✅ Shell Options and Configuration

- Viewing shell options using `set -o`
- Enabling shell options using `set -o`
- Disabling shell options using `set +o`
- Working with `allexport`
- Using `SHELLOPTS`
- Bash configuration using `.bashrc`
- Zsh configuration using `.zshrc`
- Reloading configuration using `source`
- Persisting shell options through shell configuration files

## 📂 Documentation Added

- `environment-and-processes.md`
- `shell-aliases.md`
- `shell-options-and-configuration.md`

## 🧪 Practical Work

- Created and checked local shell variables.
- Exported variables and verified them using `env`.
- Practiced environment variable inheritance in child shells.
- Created child shells using `bash` and `zsh`.
- Checked the current shell PID using `$$`.
- Used `ps -f` to examine process information.
- Identified `PID` and `PPID`.
- Returned to parent shells using `exit`.
- Created and tested shell aliases.
- Removed aliases using `unalias`.
- Viewed shell options using `set -o`.
- Enabled and disabled shell options.
- Worked with the `allexport` option.
- Checked shell option information using `SHELLOPTS`.
- Worked with `.bashrc` and `.zshrc`.
- Reloaded shell configuration using `source`.
- Practiced persisting shell settings through configuration files.

## 🚀 Progress

Week 14 has been completed with hands-on practice in shell environments, process relationships, aliases, shell options, and shell configuration.
